import {Alert,AlertDescription,AlertTitle} from '@/components/ui/alert'
import { FC} from 'react';
interface SearchProps {
    desc: string;
  }

export const Alerta:FC<SearchProps> = ({desc}) => {
    return(
        <>
            
        </>
    )
}